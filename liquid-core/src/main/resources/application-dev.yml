spring:
  jpa:
    show-sql: true

  redis:
    host: localhost
    port: 16379
    database: 0
    password:
    timeout: 1200
    jedis:
      pool:
        enabled: true
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: -1

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:liquid;MODE=MySQL;AUTO_RECONNECT=TRUE
    username: root
    password: liquid

  flyway:
    locations: classpath:db/migration/h2

logging:
  file:
    path: logs
    name: liquid-service-dev.log


#  build.version=@project.version@
#  build.timestamp=@maven.build.timestamp@
#
#  spring.profiles.active=@spring.profiles.active@
#
#  spring.mvc.throw-exception-if-no-handler-found=true
#  spring.mvc.pathmatch.matching-strategy=ant_path_matcher
#  spring.web.resources.add-mappings=false
#
#  spring.flyway.fail-on-missing-locations=true
#
#  spring.messages.basename=static.i18n.message
#  spring.messages.cache-duration=3600
#  spring.messages.encoding=UTF-8
#
#
#  jwt.encrypt-secret=cQfTjWnZr4u7w!z%C*F-JaNdRgUkXp2s5v8y/A?D(G+KbPeShVmYq3t6w9z$C&E)
#  jwt.default-expire-time=86400000
#  jwt.remember-expire-time=2678400000
